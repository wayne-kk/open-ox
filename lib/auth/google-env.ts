/**
 * Whether Google OAuth via Supabase can complete end-to-end.
 * Credentials live in Supabase Dashboard → Authentication → Providers → Google.
 */
export { isGoogleOAuthConfigured } from "@/lib/env";
