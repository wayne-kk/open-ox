# 工作台文件夹管理 — 需求（v1.0）

**状态**：已确认（grilling 2026-07-10）  
**日期**：2026-07-10  
**范围**：项目在文件夹间移动；文件夹创建/重命名/解散；侧栏与工作台 IA。  
**非范围**：批量移动、文件夹排序/嵌套、拖拽、「看全部项目」视图。

---

## 0. 已锁定决策（grilling）

| # | 决策 | 选择 |
|---|------|------|
| 1 | 移动入口 | 卡片 `⋯` →「移动到…」子菜单 |
| 2 | 移动交互 | 子菜单：最外层 + 各文件夹；**当前所在打勾/灰显** |
| 3 | 文件夹管理 | 独立「管理文件夹」面板：创建 / 重命名 / 删除（解散） |
| 4 | 「未分类」 | 去掉；无文件夹 = 最外层 |
| 5 | 「全部」/「我的项目」 | **仅最外层**（`folderId` 为空） |
| 6 | 删文件夹 | **解散**：项目回最外层，不删项目 |
| 7 | 管理入口 | 主区标题旁「管理文件夹」 |
| 8 | 主区 pill | **去掉**；筛选只靠侧栏 |
| 9 | 侧栏 | 「我的项目」= 根（可点）；展开只列各文件夹 |
| 10 | 默认 URL | 无参 / `folder=all` / 旧 `uncategorized` → 根 |
| 11 | 夹内新建 | 归入当前文件夹 |
| 12 | 解散确认 | 短文案 + 夹内 N 个项目 |
| 13 | 当前位置 | 主区标题：根「我的项目」/ 夹内文件夹名 |

---

## 1. 成功标准

- [x] 卡片菜单可将项目移到任意文件夹或最外层；当前目标不可再点
- [x] 「管理文件夹」可创建、重命名、解散；解散后项目出现在根，项目本身仍在
- [x] 侧栏无「未分类」；「我的项目」高亮仅在根；进夹只看该夹
- [x] 主区无 folder pill；有当前位置标题 +「管理文件夹」
- [x] `folder=all` / 无参 / `uncategorized` 列表均为 `folder_id IS NULL`
- [x] 在 `folder=<uuid>` 下创建的项目写入该夹

---

## 2. 工程要点

- `DELETE /api/folders/:id`：先 `folder_id → null`，再删文件夹行（替换 `deleteFolderAndProjects`）
- 新增 `PATCH /api/folders/:id`：重命名
- `listProjectsSummary`：`all` 与 `uncategorized` 均按根过滤
- `HeroPrompt`：仅 UUID 作为 `folderId`，忽略 `all` / `uncategorized`
