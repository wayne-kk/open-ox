## Rule: Output JSON

When the task requires structured data, output valid JSON only.

### Requirements

- No markdown code fences.
- No explanation before or after the JSON object.
- Use double quotes for keys and strings.
- No trailing commas.
- Return a single parseable JSON payload.