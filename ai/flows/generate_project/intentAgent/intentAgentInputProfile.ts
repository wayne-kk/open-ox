/**
 * URL helpers for intent-agent observability / future tool routing.
 * Implementation lives in {@link @/lib/reference/referenceSiteUrls}.
 */

export {
  extractHttpUrls,
  isLikelyAssetUrl,
  listReferenceSiteCandidateUrls,
} from "@/lib/reference/referenceSiteUrls";
