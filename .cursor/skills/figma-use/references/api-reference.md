# API reference notes (stub)

Document node creation, variables API, core properties, and what is supported or blocked in `use_figma`.

Prefer [plugin-api-standalone.d.ts](plugin-api-standalone.d.ts) as the source of truth for signatures.
