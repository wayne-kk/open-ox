# Variable patterns (stub)

Add: collections, modes, **scopes** (prefer explicit scopes — avoid default `ALL_SCOPES`), aliasing, binding patterns, discovering variables.

**Scope examples** (illustrative — verify against API): `FRAME_FILL`, `SHAPE_FILL`, `TEXT_FILL`, `GAP`, etc.
