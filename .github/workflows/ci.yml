name: CI / CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── CI: Typecheck (fast). Full `next build` only on PRs. ────────────────────
  check:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.5.2

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        if: github.event_name == 'pull_request'
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY: placeholder
        run: pnpm build

  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.5.2

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Run tests
        run: npx vitest --run

  # ── CD: rsync → pnpm build ON THE SERVER → pm2 reload ─────────────────────
  # Docker image builds were too heavy for this CVM (disk / layer cache / bind-mount).
  # Warm deploys skip install when lockfiles are unchanged and reuse .next/cache.
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: [check, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Generate .env.production
        run: |
          {
            echo "OPEN_OX_IMAGE_TAG=${GITHUB_SHA}"
            echo "NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}"
            echo "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY }}"
            echo "NEXT_PUBLIC_SITE_URL=${{ secrets.NEXT_PUBLIC_SITE_URL }}"
            echo "OPEN_OX_PREVIEW_BACKEND=${{ secrets.OPEN_OX_PREVIEW_BACKEND }}"
            echo "OPEN_OX_PREVIEW_CAPTURE_SECRET=${{ secrets.OPEN_OX_PREVIEW_CAPTURE_SECRET }}"
            echo "SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}"
            echo "FEISHU_APP_ID=${{ secrets.FEISHU_APP_ID }}"
            echo "FEISHU_APP_SECRET=${{ secrets.FEISHU_APP_SECRET }}"
            echo "FEISHU_OAUTH_HMAC_SECRET=${{ secrets.FEISHU_OAUTH_HMAC_SECRET }}"
            echo "FEISHU_VERIFICATION_TOKEN=${{ secrets.FEISHU_VERIFICATION_TOKEN }}"
            echo "E2B_API_KEY=${{ secrets.E2B_API_KEY }}"
            echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}"
            echo "OPENAI_API_URL=${{ secrets.OPENAI_API_URL }}"
            echo "DIFY_API_URL=${{ secrets.DIFY_API_URL }}"
            echo "DIFY_API_KEY=${{ secrets.DIFY_API_KEY }}"
            echo "ARK_API_KEY=${{ secrets.ARK_API_KEY }}"
            echo "ARK_BASE_URL=${{ secrets.ARK_BASE_URL }}"
            echo "ARK_IMAGE_MODEL=${{ secrets.ARK_IMAGE_MODEL }}"
            echo "ARK_IMAGE_SIZE=${{ secrets.ARK_IMAGE_SIZE }}"
            echo "CREDITS_ENABLED=${{ secrets.CREDITS_ENABLED || '1' }}"
            echo "CREDITS_USD_PER_CREDIT=${{ secrets.CREDITS_USD_PER_CREDIT }}"
            echo "CREDITS_MARGIN=${{ secrets.CREDITS_MARGIN }}"
            echo "STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }}"
            echo "STRIPE_WEBHOOK_SECRET=${{ secrets.STRIPE_WEBHOOK_SECRET }}"
            echo "STRIPE_PRICE_PRO_100=${{ secrets.STRIPE_PRICE_PRO_100 }}"
            echo "STRIPE_PRICE_PRO_200=${{ secrets.STRIPE_PRICE_PRO_200 }}"
            echo "STRIPE_PRICE_PRO_400=${{ secrets.STRIPE_PRICE_PRO_400 }}"
            echo "STRIPE_PRICE_TOPUP_50=${{ secrets.STRIPE_PRICE_TOPUP_50 }}"
            echo "STRIPE_PRICE_TOPUP_100=${{ secrets.STRIPE_PRICE_TOPUP_100 }}"
            echo "STRIPE_PRICE_TOPUP_200=${{ secrets.STRIPE_PRICE_TOPUP_200 }}"
            echo "VERCEL_CLIENT_ID=${{ secrets.VERCEL_CLIENT_ID }}"
            echo "VERCEL_CLIENT_SECRET=${{ secrets.VERCEL_CLIENT_SECRET }}"
            echo "VERCEL_INTEGRATION_SLUG=${{ secrets.VERCEL_INTEGRATION_SLUG }}"
            echo "VERCEL_TOKEN_ENCRYPTION_KEY=${{ secrets.VERCEL_TOKEN_ENCRYPTION_KEY }}"
          } > .env.production

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Preflight SSH connectivity
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
        run: |
          set -euo pipefail
          HOST="$SERVER_HOST"
          PORT="${SERVER_PORT:-22}"
          echo "==> Checking TCP ${HOST}:${PORT}"
          for i in 1 2 3; do
            if timeout 5 bash -c "cat < /dev/null > /dev/tcp/${HOST}/${PORT}"; then
              echo "TCP ok (attempt ${i})"
              break
            fi
            if [ "$i" -eq 3 ]; then
              echo "ERROR: cannot reach ${HOST}:${PORT}"
              exit 1
            fi
            sleep 3
          done

      - name: Add server to known_hosts
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          ssh-keyscan -p "${SERVER_PORT:-22}" -H "$SERVER_HOST" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Sync app to server
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_PORT: ${{ secrets.SERVER_PORT }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          set -euo pipefail
          APP=/sharedata/wayne/open-ox
          RSYNC_SSH="ssh -p ${SERVER_PORT:-22}"

          ssh -p "${SERVER_PORT:-22}" "$SERVER_USER@$SERVER_HOST" bash -s <<EOF
          set -euo pipefail
          mkdir -p "$APP/sites/template" "$APP/logs" "$APP/.open-ox" "$APP/.playwright/browsers"
          if [ -d "$APP/sites" ] && [ ! -w "$APP/sites/template" ]; then
            sudo chown -R "$SERVER_USER:$SERVER_USER" "$APP/sites/template" || true
          fi
          sudo chmod -R a+rwX "$APP/sites" || chmod -R a+rwX "$APP/sites" || true
          EOF

          # Incremental sync — keep server node_modules / .next / sites/{projectId}/.
          rsync -az \
            -e "$RSYNC_SSH" \
            --exclude '.git' \
            --exclude 'node_modules' \
            --exclude '.next' \
            --exclude 'coverage' \
            --exclude '.scratch' \
            --exclude '.cursor' \
            --exclude '.planning' \
            --exclude '.kiro' \
            --exclude 'docs' \
            --exclude 'e2b-template' \
            --exclude 'sites' \
            --exclude 'logs' \
            --exclude '.open-ox' \
            --exclude '.playwright' \
            --exclude '*.log' \
            ./ "$SERVER_USER@$SERVER_HOST:$APP/"

          # Template sources only (never wipe template node_modules on the server).
          rsync -az \
            -e "$RSYNC_SSH" \
            --exclude 'node_modules' \
            --exclude '.next' \
            --exclude 'out' \
            sites/template/ "$SERVER_USER@$SERVER_HOST:$APP/sites/template/"

      - name: Deploy on server (PM2)
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          timeout: 120s
          # Warm: install skip + next build + pm2 reload. Cold first run can install browsers.
          command_timeout: 45m
          script: |
            set -euo pipefail
            export OPEN_OX_APP_DIR=/sharedata/wayne/open-ox
            # Login shell so nvm/fnm from ~/.bashrc is available (appleboy is non-interactive).
            bash -lc 'cd /sharedata/wayne/open-ox && bash scripts/deploy-on-server.sh'

      - name: Notify Feishu (success)
        if: success()
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          BRANCH: ${{ github.ref_name }}
          ACTOR: ${{ github.actor }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          payload=$(jq -n \
            --arg repo "open-ox" \
            --arg branch "$BRANCH" \
            --arg actor "$ACTOR" \
            --arg msg "$COMMIT_MSG" \
            --arg run_url "$RUN_URL" \
            '{
              msg_type: "interactive",
              card: {
                header: {
                  title: { tag: "plain_text", content: "✅ 部署成功" },
                  template: "green"
                },
                elements: [
                  {
                    tag: "div",
                    fields: [
                      { is_short: true, text: { tag: "lark_md", content: ("**项目**\n" + $repo) } },
                      { is_short: true, text: { tag: "lark_md", content: ("**分支**\n" + $branch) } },
                      { is_short: true, text: { tag: "lark_md", content: ("**提交者**\n" + $actor) } },
                      { is_short: false, text: { tag: "lark_md", content: ("**提交信息**\n" + $msg) } }
                    ]
                  },
                  {
                    tag: "action",
                    actions: [
                      {
                        tag: "button",
                        text: { tag: "plain_text", content: "查看详情" },
                        url: $run_url,
                        type: "default"
                      }
                    ]
                  }
                ]
              }
            }')
          curl -X POST "$FEISHU_WEBHOOK" -H "Content-Type: application/json" -d "$payload"

      - name: Notify Feishu (failure)
        if: failure()
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          BRANCH: ${{ github.ref_name }}
          ACTOR: ${{ github.actor }}
          COMMIT_MSG: ${{ github.event.head_commit.message }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          payload=$(jq -n \
            --arg repo "open-ox" \
            --arg branch "$BRANCH" \
            --arg actor "$ACTOR" \
            --arg msg "$COMMIT_MSG" \
            --arg run_url "$RUN_URL" \
            '{
              msg_type: "interactive",
              card: {
                header: {
                  title: { tag: "plain_text", content: "❌ 部署失败" },
                  template: "red"
                },
                elements: [
                  {
                    tag: "div",
                    fields: [
                      { is_short: true, text: { tag: "lark_md", content: ("**项目**\n" + $repo) } },
                      { is_short: true, text: { tag: "lark_md", content: ("**分支**\n" + $branch) } },
                      { is_short: true, text: { tag: "lark_md", content: ("**提交者**\n" + $actor) } },
                      { is_short: false, text: { tag: "lark_md", content: ("**提交信息**\n" + $msg) } }
                    ]
                  },
                  {
                    tag: "action",
                    actions: [
                      {
                        tag: "button",
                        text: { tag: "plain_text", content: "查看日志" },
                        url: $run_url,
                        type: "danger"
                      }
                    ]
                  }
                ]
              }
            }')
          curl -X POST "$FEISHU_WEBHOOK" -H "Content-Type: application/json" -d "$payload"
